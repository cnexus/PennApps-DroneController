apply plugin: 'com.android.application'

android {
    compileSdkVersion 19
    buildToolsVersion "20.0.0"

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    repositories {
        maven {
            url 'C:\\Users\\Carlos\\Documents\\myo-android-sdk-0.10.0\\myorepository'
        }
    }

    compile('com.thalmic:myosdk:0.10.0@aar')
}
